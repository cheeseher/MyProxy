# 项目重命名与使用手册生成计划

您希望将配置文件重命名为 `By-Shane`，并需要一份详尽的使用手册，涵盖 MITM 设置和策略组选择建议。

## 1. 文件重命名

*   **当前文件**: `Surge-Dual.conf`
*   **目标文件**: `By-Shane.conf`
*   **操作**: 重命名文件，并同步更新文档中提到的托管链接。

## 2. 编写《Surge 使用手册 (By-Shane 版)》

我将创建一个名为 `README.md`（或 `User-Manual.md`）的详细文档，包含以下内容：

### A. 基础设置 (MITM)
*   **是否开启 MITM**: 强烈建议开启。
    *   **原因**: 去广告、YouTube 画中画、以及精准的 HTTPS 流量解密分流（如 TikTok 换区）都需要 MITM。
    *   **步骤**: 详细说明如何在 iOS/Mac 上安装并信任证书。

### B. 策略组最佳实践
针对每个策略组，我将给出**默认推荐**和**应急方案**：
*   **节点选择**: 默认选 `自动选择`。
*   **电报消息**: 默认选 `新加坡节点`（防断连）。
*   **Zoom会议**: 默认选 `全局直连`（国内优化），卡顿切 `香港`。
*   **群晖办公**: 默认选 `全局直连`（内网优先）。
*   **人工智能**: 默认选 `自动选择`（Surge 会自动找能用的节点），或手动锁 `美国`。
*   **GitHub**: 默认选 `香港` 或 `自动选择`。

### C. 常见问题
*   如何更新配置？
*   为什么有时候还是会有广告？
*   如何查看当前节点的延迟？

## 3. 配置文件修改预览

1.  重命名 `Surge-Dual.conf` -> `By-Shane.conf`。
2.  生成详细的 `README.md`。

准备就绪，请确认此计划，我将立即执行。
