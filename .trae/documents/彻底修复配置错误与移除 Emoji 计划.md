# 彻底修复配置错误与移除 Emoji 计划

您收到的错误提示 `规则#25使用了一个未知的策略： 2 电报消息` 证实了我的猜测：Surge 在解析某些 Emoji（如 `📲`）作为策略组名称时出现了兼容性问题（可能被错误编码为 `2` 或乱码），导致规则无法找到对应的策略组。

同时，您之前也提到了希望“图标优雅，不要 Emoji”。

因此，我决定进行一次**彻底的清理**：

## 1. 移除所有策略组名称中的 Emoji

我们将不再使用 Emoji 作为策略组名称的一部分，而是完全依赖 `icon-url` 来展示图标。这既能解决乱码报错问题，又符合您“优雅、无 Emoji”的审美需求。

*   `📲 电报消息` -> `电报消息`
*   `🚀 节点选择` -> `节点选择`
*   `🇭🇰 香港节点` -> `香港节点`
*   ... (所有组同理)

## 2. 统一更新配置文件

我将重写整个 `Surge-Dual.conf` 文件，确保：
1.  `[Proxy Group]` 定义中不含 Emoji。
2.  `[Rule]` 引用中也不含 Emoji。
3.  保持所有 `icon-url` 配置，确保视觉上依然有精美的彩色图标。

## 3. 预期效果

*   **不再报错**：纯中文/英文名称兼容性最好，杜绝“未知策略”错误。
*   **界面清爽**：面板上只显示文字名称和我们配置的高清图标，不再有 Emoji 混杂。

准备就绪，请确认此计划，我将立即执行修复。
