# 修复 Surge 配置无效问题

您遇到的错误提示是：**第 116 行存在无效配置：规则使用了未知的策略 ` 电报消息`**。

这是因为在之前的修改中，我不小心引入了一个特殊字符（编码问题），导致 `📲 电报消息` 这个策略组名称中的 Emoji `📲` 变成了乱码 ``，从而使 Surge 无法识别这个策略组。

## 1. 修复方案

我将修正 `Surge-Dual.conf` 第 116 行的策略组名称，确保它与 `[Proxy Group]` 定义中的名称完全一致。

*   **错误**: `...Telegram.list, 电报消息`
*   **正确**: `...Telegram.list,📲 电报消息`

## 2. 配置文件修改预览

我将执行一个简单的替换操作，将那个乱码字符修复回来。

准备就绪，请确认此计划，我将立即修复。
